# Stable Work Packages

## WP1 — Frozen shared contract and failing baseline

- Add FAQ mode/document IDs to backend/frontend block types and a resolved FAQ projection shape.
- Preserve legacy documents with deterministic defaults.
- Record the current failing baseline in the standard run ledger.
- Acceptance: malformed modes/IDs fail, identity invariant remains, baseline identifies current manual FAQ/duplicate preview/scroll defects.

## WP2 — Canonical data resolution and creation services

- Resolve FAQ/business/case/identity through company-scoped services for draft preview and public output.
- Complete default/copy/customize-before-create behavior and employee relationship validation.
- Make avatar write-through explicit and audited.
- Acceptance: success and negative API/service tests for published/public/current/cross-company/unbound cases.

## WP3 — Shared mobile renderer

- Move visitor-visible block presentation to a shared boundary consumed by admin and card-web.
- Restore count-aware business, case, gallery and FAQ layouts plus existing interactive actions.
- Acceptance: identical ordered inputs produce equivalent editor/public module trees and interactions.

## WP4 — Three-pane editor and data inspectors

- Replace nested dialog scrolling with viewport-bound left/center/right panes.
- Add synchronized selection, pointer/keyboard dragging, derived directory and FAQ auto/selected inspector.
- Acceptance: pane scroll isolation, identity invariants, immediate synchronized reorder and no manual FAQ answer field.

## WP5 — Creation/default flow and employee identity UX

- Present default, copy and customize choices before persistence.
- Require an active enterprise employee and expose avatar write-through with scope messaging.
- Acceptance: canceling customization leaves no new card; default flow avoids redundant editing.

## WP6 — Integrated verification

- Run focused tests once per affected package, builds once after integration and one real-browser end-to-end pass.
- Acceptance: every Eval Contract hard gate maps to current direct evidence; related console/network errors are zero.
